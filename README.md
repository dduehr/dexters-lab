# Dexters's Lab

## Backend

Install and start the Express backend:

```sh
cd backend
npm install
npm start
```

## Frontend

Install and run the React frontend:

```sh
cd frontend
npm install
npm run dev
```

